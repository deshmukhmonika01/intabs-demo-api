# intabs-demo-api
Sample Spring Boot API with In Memory H2 DB

## Run Locally
```sh
~mvn spring-boot:run
```

## Other ways to Run
```sh
~mvn clean compile install
~java -jar target/<name of jar> 
```
